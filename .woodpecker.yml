when:
  - event: [push, pull_request]

steps:
  - name: test
    image: python:3.13-slim
    commands:
      - pip install -e ".[dev]" -q
      - pytest tests/ -v --cov=githost_mcp --cov-report=term-missing --cov-fail-under=60

  - name: pip-audit
    image: python:3.13-slim
    commands:
      - pip install pip-audit -q
      - pip install -e "." -q
      - pip-audit
